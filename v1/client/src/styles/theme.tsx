import { createTheme } from "@mui/material";

export const projectTheme = createTheme({
  palette: {
    primary: {
      light: "#FFE54C",
      main: "#FDCA00",
      dark: "#C7A100",
    },
    secondary: {
      light: "#3E392E",
      main: "#151100",
      dark: "#000000",
    },
  },
});
